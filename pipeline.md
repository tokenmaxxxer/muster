# 파이프라인 계약

라우터·워커·사람이 공유하는 규칙. 라우터 코드가 아니라 이 문서가 계약이다.

## 라벨 상태기계

```
사람: 이슈 발행 + pipeline:plan
  → [plan]   spec.md            → pipeline:build
  → [build]  PR (Closes #N)     → pipeline:review
  → [review] 기계 게이트 → 계약 검증 → 머지 → pipeline:qa
                                기각  → pipeline:build (attempt+1)
  → [qa]     run record         → pipeline:done
                                실패  → pipeline:build (attempt+1)
```

| 라벨 | 의미 |
|---|---|
| `pipeline:<stage>` | 배차 대기 |
| `pipeline:<stage>:running` | 배차됨. 중복 스폰 락 |
| `attempt:N` | 루프 예산. `max_attempts` 도달 시 에스컬레이션 |
| `pipeline:human` | 에스컬레이션. **라우터는 손대지 않는다** |
| `pipeline:done` | 종료 |

파이프라인 라벨은 **항상 하나만** 유지된다. 둘이 붙어 있으면 `:running` 이 이긴다
(중복 스폰 방지).

## 불변식

1. **워커는 GitHub 토큰을 갖지 않는다.** 게시(코멘트·PR·라벨·머지)는 라우터만 한다.
   GitHub 은 코멘트 권한과 라벨 권한을 분리하지 않으므로, 워커에게 토큰을 주면
   워커가 `pipeline:human` 을 떼어내 자기 게이트를 무력화할 수 있다.
2. **라우터는 산출물의 존재만 본다.** 품질은 다음 스테이지·게이트·사람이 판정한다.
   유일한 예외는 review 의 `verdict.json` 인데, 이것도 워커가 낸 불리언을 전사할 뿐이다.
3. **상태는 라벨에 있다.** 라우터가 죽어도 라벨이 남고, 재시작하면 이어진다.
   사람이 라벨만 바꿔도 개입(재개·건너뛰기·중단)이 된다.
4. **게이트는 우회 불가.** 게이트가 라벨 전이의 조건이므로 라우터도 건너뛸 수 없다.

## 사람의 개입 지점

- `pipeline:human` 이 붙은 이슈. 라우터는 이 라벨을 보면 아무것도 하지 않는다.
- 재개: 라벨을 원하는 스테이지로 되돌리고 `attempt:N` 을 지운다.
- 릴리스: 머지는 자율이지만 **배포는 이 파이프라인 밖**이다. 사람 게이트는
  머지가 아니라 릴리스에 있다.

## 기계 게이트

| 게이트 | 막는 것 | 실패 시 |
|---|---|---|
| `writeset` | 보호 경로 변경, spec 이 선언한 write-set 이탈 | 에스컬레이션 |
| `deps` | 레지스트리에 없는 패키지(환각), 확인 불가(fail closed) | 에스컬레이션 |
| `attempt` | 무한 회귀 | 에스컬레이션 |

**보호 경로** (`src/gates.py:PROTECTED`) 는 CI 설정·시크릿·인증·마이그레이션과
`adapters.yml`·`pipeline.md`·`profiles/` 자신을 포함한다. 파이프라인이 자기 규칙을
다시 쓸 수 없어야 한다.

게이트 실패는 **재시도가 아니라 에스컬레이션**이다. 재시도해도 같은 결과가 나오고,
게이트가 막았다는 것은 사람이 봐야 한다는 뜻이다.

## 어댑터 작성 규칙

E2E 에서 실제로 물린 것들. 지키지 않으면 조용히 깨진다.

1. **프롬프트는 stdin 으로.** `--add-dir`·`--allowedTools` 같은 가변 인자 플래그가
   뒤따르는 프롬프트를 인자로 삼킨다.
2. **신뢰할 수 없는 값은 `printf` 의 인자로.** 히어독이나 따옴표 없는 확장에 넣으면
   이슈 제목의 `$(...)` 가 실행된다. 이슈 제목은 누구나 쓸 수 있다.
3. **계약은 경로가 아니라 내용으로.** 워커의 샌드박스 허용 디렉터리는 `work/` 와
   `out/` 뿐이라 그 밖의 경로를 주면 읽지 못하고 멈춘다. 셸은 라우터 권한으로
   읽으므로 `"$(cat "$SPEC")"` 로 내용을 넘긴다.
4. **워커 격리는 어댑터 문자열이 담는다.** 라우터는 컨테이너를 모른다:
   `docker run --rm --network stage-build -v … img claude -p …`

## dispatch 규율과의 충돌 (미해결)

`dispatch` 플러그인은 "PR 은 명시적으로 기록된 사용자 승인에서만 머지된다"를
요구한다. 이 파이프라인의 review 스테이지는 그 승인을 크로스모델 에이전트가 낸
판정으로 대체한다. 현재는 판정 사유를 이슈 코멘트로 남겨 "기록된 승인"의 형식을
지키고 있으나, dispatch 쪽에 자율 파이프라인 예외를 명시할지 결정이 필요하다.
