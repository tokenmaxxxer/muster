---
allowed-tools: Bash(python3:*), Bash(git remote:*), Bash(git status:*), Read
description: 역할별 에이전트를 띄운다. 인자 없이 부르면 지금 상태를 보여준다
argument-hint: "[역할 [맡길 일]] — 예: qa /testrun:testrun smoke | 비우면 상태만"
---

인자: $ARGUMENTS

`MUSTER=${CLAUDE_PLUGIN_ROOT}/..` 로 두고, 아래는 전부 `python3 $MUSTER/spawn.py` 를 쓴다.

## 인자가 없으면 — 상태만 보고한다

```
python3 $MUSTER/spawn.py -C <현재 작업 디렉터리>
```

출력을 그대로 옮기지 말고 **읽어서 한 문단으로** 말해라: 어떤 프로젝트인지, 진행
중인 역할이 있는지, 없으면 그렇다고. 그리고 지금 맡길 만한 일이 보이면 하나만
제안한다. 목록을 늘어놓지 마라.

**아무것도 띄우지 않는다.** 상태 보고로 끝난다.

## 역할과 일이 주어지면 — 띄운다

```
python3 $MUSTER/spawn.py <역할> "<맡길 일>" -C <작업 디렉터리>
```

띄우기 전에 확인할 것:

1. **역할이 맞는가.** `qa` 는 제품을 실행하고, `coding` 은 코드를 쓰고, `review` 는
   읽기만 한다. 요청과 역할이 어긋나면 띄우지 말고 되물어라.
2. **작업 디렉터리가 맞는가.** 기본값은 현재 디렉터리다. 대상 레포가 따로면 `-C` 로
   준다. 엉뚱한 레포에서 `coding` 을 띄우면 그 레포에 코드를 쓴다.
3. **맡길 일이 룰북 커맨드면 그대로 넘긴다.** 예: `/testrun:testrun smoke`.
   프롬프트를 새로 지어내지 마라 — 판단은 룰북에 있다.

`spawn.py` 가 미설치 플러그인을 발견하면 멈춘다. 그 경우 **한 번 더 실행**하면
설치가 끝나 있다(첫 실행은 마켓플레이스 등록만 한다). 두 번 실패하면 그대로 보고해라.

## 하지 않는 것

- **상태 파일을 고치지 않는다.** `state.md` 는 그 룰북의 플러그인만 쓴다. 전이가
  필요하면 해당 역할을 띄워서 그 룰북이 하게 한다. 밖에서 고치면 전이 게이트를
  우회하는 것이고, 사람 전용 전이의 승인 토큰도 건너뛴다.
- **역할을 여러 개 동시에 띄우지 않는다.** 하나 끝나고 결과를 보고 다음을 정한다.
- **결과를 각색하지 않는다.** 에이전트가 실패했으면 실패했다고 말한다.
